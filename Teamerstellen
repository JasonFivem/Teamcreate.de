<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Team Ersteller</title>
    <style>
      /* Hier kommt dein CSS */
      .team-container {
        display: flex;
        flex-wrap: wrap;
        justify-content: center;
      }
      .team-card {
        width: 300px;
        height: 400px;
        background-color: white;
        border: 1px solid black;
        margin: 20px;
        box-shadow: 2px 2px 5px rgba(0, 0, 0, 0.1);
        text-align: center;
      }
      .team-header {
        background-color: lightgray;
        height: 50px;
        line-height: 50px;
        font-size: 24px;
        font-weight: bold;
      }
      .team-body {
        padding: 20px;
      }
      .team-member {
        margin-top: 20px;
        font-size: 18px;
      }
      .team-form {
        display: flex;
        flex-wrap: wrap;
        justify-content: center;
        margin-top: 20px;
      }
      .team-form input[type="text"] {
        width: 300px;
        height: 40px;
        padding: 10px;
        font-size: 18px;
        margin-right: 10px;
      }
      .team-form input[type="submit"] {
        width: 100px;
        height: 40px;
        background-color: lightgray;
        border: none;
        font-size: 18px;
        cursor: pointer;
      }
    </style>
  </head>
  <body>
    <h1>Team Ersteller</h1>
    <div class="team-form">
      <input type="text" id="team-name" placeholder="Name des Teams">
      <input type="submit" value="Team erstellen" id="create-team-btn">
    </div>
    <div class="team-container">
      <!-- Hier kommen die Teams hin -->
    </div>
    <script>
      // Hier kommt dein JavaScript
      const createTeamBtn = document.querySelector("#create-team-btn");
      const teamContainer = document.querySelector(".team-container");
      const teamNameInput = document.querySelector("#team-name");
      let teamId = 0;

      createTeamBtn.addEventListener("click", function(event) {
        event.preventDefault();
        const teamName = teamNameInput.value;
        if (!teamName) {
          return alert("Bitte gib einen Namen für das Team ein.");
        }

        const teamCard = document.createElement("div");
        teamCard.classList.add("team-card");
        teamCard.innerHTML = `
          <div class="team-header">${
