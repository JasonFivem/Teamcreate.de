<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Team Ersteller</title>
    <style>
      /* Hier kommt dein CSS */
      .team-container {
        display: flex;
        flex-wrap: wrap;
        justify-content: center;
      }
      .team-card {
        width: 300px;
        height: 400px;
        background-color: white;
        border: 1px solid black;
        margin: 20px;
        box-shadow: 2px 2px 5px rgba(0, 0, 0, 0.1);
        text-align: center;
      }
      .team-header {
        background-color: lightgray;
        height: 50px;
        line-height: 50px;
        font-size: 24px;
        font-weight: bold;
      }
      .team-body {
        padding: 20px;
      }
      .team-member {
        margin-top: 20px;
        font-size: 18px;
      }
      .team-input {
        width: 200px;
        height: 30px;
        margin-top: 20px;
        font-size: 18px;
      }
      .team-btn {
        margin-top: 20px;
        font-size: 18px;
        padding: 10px 20px;
        background-color: lightgray;
        border: none;
        border-radius: 5px;
        cursor: pointer;
      }
    </style>
  </head>
  <body>
    <h1>Team Ersteller</h1>
    <p>Erstelle dein eigenes Team:</p>
    <input type="text" class="team-input" id="team-name" placeholder="Name des Team">
    <button class="team-btn" id="create-team-btn">Team erstellen</button>
    <div class="team-container">
      <!-- Hier kommen die Teams hin -->
    </div>
    <script>
      // Hier kommt dein JavaScript
      const createTeamBtn = document.querySelector("#create-team-btn");
      const teamContainer = document.querySelector(".team-container");
      const teamNameInput = document.querySelector("#team-name");
      let teamId = 0;

      createTeamBtn.addEventListener("click", function() {
        const teamName = teamNameInput.value;
        if (!teamName) {
          return alert("Bitte gib einen Namen für das Team ein.");
        }

        const teamCard = document.createElement("div");
        teamCard.classList.add("team-card");
        teamCard.innerHTML = `
          <div class="team-header">${teamName}</div>
          <div class="team-body">
            <p class="team-member">Mitglied 1</p>
            <p class="team-member">Mitglied 2</p>
            <p class="team-member">Mit
